import React from 'react';
import ReactDOM from 'react-dom/client';
import RentApp from './RentApp';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <RentApp />
  </React.StrictMode>,
);
